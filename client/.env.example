# Only REACT_APP_* variables are exposed to the browser — NEVER put secrets here.
# This points the client at our own Express backend (which proxies Amadeus).
REACT_APP_API_URL=http://localhost:5000/api
